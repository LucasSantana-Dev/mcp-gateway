# Tool Router runtime dependencies
# These are the core packages required to run the tool_router module.
# For development/test dependencies, see pyproject.toml [project.optional-dependencies.dev]
mcp
mcp-contextforge-gateway
httpx
psutil>=5.9.0
