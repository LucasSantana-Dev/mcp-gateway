# Gateways: Name|URL|Transport (optional). Stack: React, Node.js, TypeScript, JavaScript, Java, Spring, Tailwind, Next.js, Jest, Prisma.
# Local lines use SSE; remote lines use STREAMABLEHTTP. Transport column can be omitted (script infers from URL).
# Local translate services (reachable by the gateway). Start with ./start.sh then run this script.
sequential-thinking|http://sequential-thinking:8013/sse|SSE
chrome-devtools|http://chrome-devtools:8014/sse|SSE
playwright|http://playwright:8015/sse|SSE
magicuidesign-mcp|http://magicuidesign-mcp:8016/sse|SSE
desktop-commander|http://desktop-commander:8017/sse|SSE
puppeteer|http://puppeteer:8018/sse|SSE
browser-tools|http://browser-tools:8019/sse|SSE
tavily|http://tavily:8020/sse|SSE
filesystem|http://filesystem:8021/sse|SSE
reactbits|http://reactbits:8022/sse|SSE
snyk|http://snyk:8023/sse|SSE
uiforge|http://uiforge:8026/sse|SSE
memory|http://memory:8027/sse|SSE
# git-mcp|http://git-mcp:8028/sse|SSE  # Temporarily commented: connection reset errors
fetch|http://fetch:8029/sse|SSE
tool-router|http://tool-router:8030/sse|SSE
postgres|http://postgres:8031/sse|SSE
mongodb|http://mongodb:8032/sse|SSE
# sqlite/github often fail with "Unable to connect" or "Unexpected error"; uncomment after checking docker compose logs sqlite|github, or add via Admin UI.
# sqlite|http://sqlite:8024/sse|SSE
github|http://github:8025/sse|SSE
# NOTE: github gateway is required for most stack profiles (nodejs-typescript, react-nextjs, etc.)
# Configure GITHUB_PERSONAL_ACCESS_TOKEN in your IDE's mcp.json, NOT in .env
# See docs/IDE_SETUP_GUIDE.md for configuration examples

# Remote (stack): uncomment to register via script, or add to EXTRA_GATEWAYS in .env. Context7 needs API key in Admin UI (Passthrough Headers).
Context7|https://mcp.context7.com/mcp|STREAMABLEHTTP
DeepWiki|https://mcp.deepwiki.com/mcp|STREAMABLEHTTP
prisma-remote|https://mcp.prisma.io/mcp|STREAMABLEHTTP
# Often unreachable or 406 from gateway; add via Admin UI if needed.
# context-awesome|https://www.context-awesome.com/api/mcp|STREAMABLEHTTP
# cloudflare-observability|https://observability.mcp.cloudflare.com/mcp|STREAMABLEHTTP
# cloudflare-bindings|https://bindings.mcp.cloudflare.com/mcp|STREAMABLEHTTP
# v0|https://mcp.v0.dev|STREAMABLEHTTP

# Remote (auth required): add in Admin UI and set Passthrough Headers or OAuth. Do not put secrets in this file.
# Commented: require manual authentication setup in Admin UI (Passthrough Headers or OAuth)

# Removed servers (not recommended):
# - context-awesome: Returns HTTP 406 due to upstream Context Forge limitation (missing Accept header)
# - prisma-remote: Unreliable, similar upstream issues
# - apify-dribbble: Niche use case, low value for general development
